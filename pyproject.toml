[project]
name = "arm-for-raspberrypi"
version = "0.1.0"
description = "Automatische CD-Ripping-Web-UI für Raspberry Pi 5 unter Linux"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
arm-ripper = "arm_ripper.server:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
